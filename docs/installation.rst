============
Installation
============

At the command line::

    $ easy_install django-wlcmanager

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-wlcmanager
    $ pip install django-wlcmanager
